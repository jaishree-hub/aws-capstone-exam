[defaults]
inventory = inventory.ini
host_key_checking = False
remote_user = ec2-user

# ⭐ CHANGE HERE: your PEM file name
private_key_file = ~/.ssh/06-feb

# Optional (nice to have)
stdout_callback = yaml
timeout = 60
